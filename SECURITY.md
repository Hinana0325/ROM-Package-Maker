# 安全策略

## 支持的版本

| 版本    | 支持状态  |
| ----- | ----- |
| 0.1.x | ✅ 支持  |
| < 0.1 | ❌ 不支持 |

## 反馈安全漏洞

我们非常重视本项目的安全。如发现安全漏洞，请**不要**通过公开 Issue 提交，而是：

1. 通过 GitHub 私密安全公告（Security → Report a vulnerability）提交；或
2. 联系维护者邮箱（待补充：`security@example.com`）。

反馈时请尽量包含：

- 问题类型（如路径穿越、任意代码执行、签名校验绕过）

- 复现步骤 / PoC

- 影响范围评估

我们将在收到报告后尽快确认，并在修复发布后致谢报告者（除非要求匿名）。

## 处理原则

- 刷机包签名已落地：默认使用内置**自签名测试证书**（仅供测试，不可用于正式分发）；自定义密钥（.pk8 / .pfx / .pem）路径在应用设置中配置，密钥文件不入库（`.gitignore` 已排除 `*.pfx` / `*.cer`）

- 解包 ROM 属于处理不可信输入：ext4 / sparse / boot / super 解析器需防御畸形镜像结构与路径穿越（如 zip 内 `../` 条目、损坏的 extent / inode）

- 使用自定义签名密钥时，密钥仅在签名阶段读取，不写入日志与设置明文之外的任何位置

