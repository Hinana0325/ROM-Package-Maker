<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="RomPackageMaker.Pages.BuildPropPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:RomPackageMaker.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:services="using:RomPackageMaker.Engine"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Build.prop 编辑器" />

        <!-- 工具栏 -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Name="OpenButton"
                Click="OpenButton_Click"
                Content="打开文件">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="O" Modifiers="Control" />
                </Button.KeyboardAccelerators>
            </Button>
            <Button
                x:Name="FindInWorkspaceButton"
                Click="FindInWorkspaceButton_Click"
                Content="从工作目录查找" />
            <Button
                x:Name="SaveButton"
                Click="SaveButton_Click"
                Content="保存"
                IsEnabled="False"
                Style="{StaticResource AccentButtonStyle}">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="S" Modifiers="Control" />
                </Button.KeyboardAccelerators>
            </Button>
            <Rectangle
                Width="1"
                Height="24"
                VerticalAlignment="Center"
                Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
            <TextBox
                x:Name="SearchBox"
                Width="260"
                VerticalAlignment="Center"
                PlaceholderText="搜索属性键或值..."
                TextChanged="SearchBox_TextChanged" />
            <Button
                x:Name="AddButton"
                Click="AddButton_Click"
                Content="添加属性"
                IsEnabled="False" />
            <Rectangle
                Width="1"
                Height="24"
                VerticalAlignment="Center"
                Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
            <ComboBox
                x:Name="PresetCombo"
                Width="220"
                VerticalAlignment="Center"
                Header="应用预设"
                ItemsSource="{x:Bind PresetNames}"
                PlaceholderText="选择参数预设..."
                SelectedIndex="0" />
            <Button
                x:Name="ApplyPresetButton"
                VerticalAlignment="Bottom"
                Click="ApplyPresetButton_Click"
                Content="应用"
                IsEnabled="False"
                ToolTipService.ToolTip="已存在的键修改值，不存在的键追加；应用后需手动保存" />
        </StackPanel>

        <!-- 列表 / 空状态 -->
        <Grid Grid.Row="2" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 状态条 -->
            <TextBlock
                x:Name="StatusText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="未打开文件" />

            <!-- 空状态引导 -->
            <Border
                x:Name="EmptyState"
                Grid.Row="1"
                Padding="32"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock
                        HorizontalAlignment="Center"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="尚未打开 build.prop" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextAlignment="Center"
                        TextWrapping="Wrap">
                        打开任意 build.prop 文件，或从解包工作目录中自动查找。
                        <LineBreak />支持搜索、编辑、新增属性；保存时自动创建 .bak 备份。
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 属性列表 -->
            <Grid x:Name="EditorArea" Grid.Row="1" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Padding="12,0,12,4" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="340" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="属性键" />
                    <TextBlock
                        Grid.Column="1"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="值" />
                </Grid>

                <ListView
                    x:Name="PropList"
                    Grid.Row="1"
                    ItemsSource="{x:Bind View, Mode=OneWay}"
                    SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="MinHeight" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="services:BuildPropLine">
                            <Grid
                                Padding="12,4,12,4"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1"
                                ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="340" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Consolas"
                                    Text="{x:Bind Key}"
                                    TextTrimming="CharacterEllipsis"
                                    ToolTipService.ToolTip="{x:Bind Description, Mode=OneTime}" />
                                <TextBox
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    FontFamily="Consolas"
                                    Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextChanged="Value_TextChanged" />
                                <Button
                                    Grid.Column="2"
                                    Padding="8,6"
                                    VerticalAlignment="Center"
                                    Click="DeleteRow_Click"
                                    Content="&#xE74D;"
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    ToolTipService.ToolTip="删除该属性" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
